---title: "CSS 'has' Selector"HTML Header:	<link rel="stylesheet" type="text/css" 					media="all" href="../assets/prism.css"/>				<script type="text/javascript" 					src="../assets/prism.js"></script>				<link rel="stylesheet" type="text/css" media="all" href="../assets/mdmd.css"/>---The CSS selector body > h1:first-of-type:has(h1,h2,h3,h4,h5,h6){} is invalid because the element being selected (h1) is on the left side of the :has() pseudo-class, and heading elements (h1 through h6) cannot contain other heading elements as children in well-formed HTML. Here is a breakdown of the selector's components:body > h1: This selects any <h1> element that is a direct child of the <body> element.:first-of-type: This structural pseudo-class further narrows the selection to the first <h1> element among its siblings (other <h1> tags) within the <body>.:has(h1,h2,h3,h4,h5,h6): This is the "parent selector" pseudo-class. It attempts to select the <h1> element only if it contains any of the specified heading elements (h1 through h6) as descendants.{}: The declaration block is empty, meaning even if a valid match was found, no styles would be applied. The fundamental issue is that HTML specifications dictate a semantic structure where heading elements (<h1> - <h6>) represent a section heading and should contain only phrasing content, not other headings. Therefore, an <h1> element can never contain an <h2> or any other heading tag, making the :has(...) condition impossible to satisfy in a valid HTML document. 	If the goal was to style the main <h1> if it is followed by other headings, a different selector combination using the sibling combinator ( + or ~ ) might be used, combined with :has() (e.g., h1:has(+ h2) {}), but the original selector's structure is fundamentally flawed. You can find more information on selectors on the MDN Web Docs documentation		____body > h1:first-of-type, body > h2:first-of-type {  /* CSS declarations */}____/* Selects <h1> only if it is followed by another heading */h1:has(~ h1, ~ h2, ~ h3, ~ h4, ~ h5, ~ h6) {  color: red;}____# Conversation: CSS Selectors for Complex Heading Relationships### User Query 1Only select this `<h1>` if it contains at least one other heading (`<h1>` through `<h6>`) after it.### Assistant ResponseIn standard CSS, it is **not possible** to select a preceding element based on a subsequent sibling without the `:has()` pseudo-class.**Modern CSS (2025):**```css/* Selects <h1> only if it is followed by another heading later in the parent */h1:has(~ :is(h1, h2, h3, h4, h5, h6)) {  color: red;}____Only select this <h1> if it contains at least one other heading (<h1> through <h6>) after itIn standard CSS, it is not possible to select a parent or preceding element based on the existence of a subsequent sibling using a single selector. CSS selectors generally only look forward or down the DOM tree. However, you can achieve this using the :has() pseudo-class in modern browsers or JavaScript. 1. CSS (Modern Browsers)The :has() selector allows you to check for the presence of a following sibling using the subsequent-sibling combinator (~). css/* Selects <h1> only if it is followed by another heading */h1:has(~ h1, ~ h2, ~ h3, ~ h4, ~ h5, ~ h6) {  color: red;}Use code with caution.2. JavaScript (Legacy Browser Support)If you need to support older browsers (like Internet Explorer or older versions of Firefox), use the querySelectorAll method to iterate through the elements. javascriptconst h1s = document.querySelectorAll('h1');h1s.forEach(h1 => {  // Check if any heading exists after this h1  let nextEl = h1.nextElementSibling;  while (nextEl) {    if (/^H[1-6]$/.test(nextEl.tagName)) {      h1.classList.add('has-subsequent-heading');      break;    }    nextEl = nextEl.nextElementSibling;  }});Use code with caution.Browser Support NoteAs of 2025, the :has() selector is supported in all major evergreen browsers (Chrome, Edge, Safari, and Firefox). It is the recommended method for modern web development. Can the :has() selector have limitations?What are some limitations of CSS selectors?Tell me more about the parent selector concept18 sitesGoogle Tag Manager CSS selector, DOM & Nodes ExplainedOct 24, 2012 — When you use a CSS selector you can find elements only forward in a DOM hierarchy.Optimize SmartUsing the DOM like a ProSep 1, 2019 — Going up the DOM tree Using CSS selectors for selecting DOM elements means we can only travel down the DOM tree. There ...Danny MoerkerkeCSS SelectorsFeb 19, 2025 — One-Way Traversal: CSS selectors primarily traverse the DOM tree from parent to child, not vice versa. They operate on...NetlifyShow allbody > h1:first-of-type:has(~ h1, ~ h2, ~ h3, ~ h4, ~ h5, ~ h6) {}Your selector is correct and highly specific. It will effectively target the first <h1> in the document body, but only if at least one other heading exists later in the same parent container. Breakdown of how it works:body > h1:first-of-type: Targets only the very first <h1> that is a direct child of the <body>.:has(...): The "relational pseudo-class" checks if the condition inside the parentheses is met.~ h1, ~ h2, ...: The subsequent-sibling combinator (~) looks for any heading level that appears anywhere after that specific <h1> within the same parent. Optimization TipIf you want to target any heading level (h1–h6) that follows without listing them all individually, you can use the CSS :is() pseudo-class inside the :has() to keep the code cleaner: cssbody > h1:first-of-type:has(~ :is(h1, h2, h3, h4, h5, h6)) {  /* Your styles here */}Use code with caution.Browser Compatibility (2025)As of late 2023 and throughout 2025, this selector has full support in all major "evergreen" browsers (Chrome 105+, Safari 15.4+, Firefox 121+, and Edge 105+). You can verify current support statistics on Can I Use for :has(). What are some practical uses of CSS :has() with sibling selectors in web design?Give me an example where this specific selector is usefulWhat other selectors can be used with :has()?11 sitesCS4640 - WebPLMay 21, 2021 — Introduction to CSS To compute the specificity, the most common way of representing this is as four-category integers ...The University of VirginiaAdvanced Test Automation: WDIO with BDD CucumberJan 21, 2025 — Verify the selector is correct.JigNectUsing !important in CSS is generally considered bad practice for several reasons:Dec 18, 2023 — Rethink your selector specificity: Ensure your desired selector has higher specificity than any conflicting styles.LinkedInShow allgive me multiple examples in html and multiple examples in markdown that would and wouldn’t targeted.To be targeted by body > h1:first-of-type:has(~ h1, ~ h2, ~ h3, ~ h4, ~ h5, ~ h6), an <h1> must meet three specific criteria: it must be a direct child of the <body>, it must be the first <h1> in that parent, and it must have another heading (h1-h6) appearing anywhere after it within that same parent.HTML Examples✅ TargetedThe first <h1> is followed by another heading in the same parent.html<body>  <h1>This IS Targeted</h1>  <p>Some text.</p>  <h2>A following sub-heading</h2></body>Use code with caution.html<body>  <h1>This IS Targeted</h1>  <h1>Another H1 later in the body</h1></body>Use code with caution.❌ NOT TargetedThe first <h1> has no subsequent headings, or it is not a direct child of the <body>.html<!-- No headings follow the h1 --><body>  <h1>NOT Targeted</h1>  <p>Only paragraphs follow.</p></body>Use code with caution.html<!-- The h1 is inside a <main> tag, not a direct child of <body> --><body>  <main>    <h1>NOT Targeted</h1>    <h2>Followed by h2, but h1 is not direct child of body</h2>  </main></body>Use code with caution.Markdown ExamplesWhen Markdown is rendered to HTML, most parsers place all generated content directly inside a container (like <body> or a <div>). Assuming the output is direct-child content of the <body>, the following logic applies:✅ Targetedmarkdown# This IS TargetedThis is the first heading, and a sub-heading follows it.## Sub-headingUse code with caution.markdown# This IS TargetedMultiple top-level headings are present.# Another HeadingUse code with caution.❌ NOT Targetedmarkdown# NOT TargetedThis is the only heading in the document.Use code with caution.markdownThis is just a paragraph.## This is an H2(The selector only targets H1 elements)Use code with caution.Key Technical RuleThe :first-of-type pseudo-class specifically targets the first occurrence of that element type among its siblings. If you have two <h1> tags, only the first one can ever be "Targeted" by this specific selector; the second one will never match because it is not the :first-of-type.Give me an example of a specific style that could be applied to a targeted h1What are some practical use cases for this CSS selector?Explain what a 'subsequent-sibling combinator' does in CSSundefinedundefinedundefined4 sites:first-of-type | CSS-TricksSep 13, 2024 — Sara Cope on Mar 12, 2013 Updated on Sep 13, 2024. The :first-of-type selector in CSS allows you to target the first o...CSS-TricksGetting Started | Markdown GuideFor example, to denote a heading, you add a number sign before it (e.g., # Heading One ). Or to make a phrase bold, you add two as...Markdown GuideCSS selector for first heading of the highest level?Jul 27, 2018 — h1:first-of-type, h2:first-of-type, h3:first-of-type, h4:first-of-type { color:red; } h1:first-of-type ~ * { display:n...Stack OverflowShow allwhat would target only the first h1 that occurs where there is an h2 on the next line?To target only the first <h1> that is immediately followed by an <h2> (the "next line" in rendered HTML), use the following CSS selector:cssh1:first-of-type:has(+ h2) {  /* Your styles here */}Use code with caution.Breakdown of the Selectorh1:first-of-type: Ensures only the very first <h1> in its parent container is considered.:has(...): A relational pseudo-class that checks for a specific condition relative to the targeted element.+ h2: The adjacent sibling combinator (+) specifically looks for an <h2> that is the immediate next sibling. Examples✅ TargetedThe <h1> is the first of its type and has an <h2> directly after it.HTML:html<body>  <h1>This IS Targeted</h1>  <h2>Sub-heading directly follows</h2></body>Use code with caution.Markdown:markdown# This IS Targeted## Sub-heading directly followsUse code with caution.❌ NOT TargetedThe <h1> is either not the first one, or there is something between it and the <h2>.HTML (Not immediate):html<body>  <h1>NOT Targeted</h1>  <p>This paragraph breaks the "next line" connection.</p>  <h2>Sub-heading</h2></body>Use code with caution.Markdown (Not the first H1):markdown# First H1 (No H2 here)# Second H1## Sub-heading follows, but this is the second H1Use code with caution.HTML (Not first-of-type):html<body>  <p>Intro text</p>  <h2>Sub-heading</h2> <!-- This is not an H1 -->  <h1>NOT Targeted</h1>  <h2>Follows an H1, but this H1 is preceded by an H2/P elsewhere</h2></body>Use code with caution.Browser Support (2025)This selector is fully supported in all major modern browsers as of 2025, including Chrome, Safari, Firefox, and Edge. If you must support legacy browsers (like Internet Explorer), you will need to use a JavaScript-based solution. How can I style the H2 instead?Give an example using :is() and the adjacent sibling combinatorHow can I use this to remove default spacing after an H1?undefinedundefinedundefined6 sitesNext-sibling combinator - CSS - MDN Web DocsDec 16, 2025 — This feature is well established and works across many devices and browser versions. It's been available across browse...MDN Web Docshas() - CSS - MDN Web DocsDec 16, 2025 — This example shows two similar texts side-by-side for comparison – the left one with an H1 heading followed by a parag...MDN Web DocsCSS: select first-of-class with the subsequent sibling combinatorMar 18, 2018 — This can be solved with a technique created by Daniel Tan and Lea Verou and shared on Stack Overflow. Rather than usin...Phil NashShow allwhat would target only the first h1 that occurs where there is an h2 or h3 on the next line?To target only the first <h1> that is immediately followed by either an <h2> or an <h3>, use the CSS :has() pseudo-class combined with the adjacent sibling combinator (+) and the :is() grouping pseudo-class. The Recommended CSS Selector (2025)cssh1:first-of-type:has(+ :is(h2, h3)) {  /* Your styles here */}Use code with caution.Breakdown of how it works:h1:first-of-type: Ensures you only target the very first <h1> within its parent container.:has(...): Checks if the targeted element has a relationship with another element.+: The adjacent sibling combinator requires the next element to be on the immediate next "line" (next in the DOM).:is(h2, h3): Acts as an "OR" statement, matching if the next element is either an <h2> or an <h3>. Targeted vs. NOT Targeted Examples✅ TargetedHTML:html<body>  <h1>This IS Targeted</h1>  <h2>Next heading is an H2</h2></body>Use code with caution.Markdown:markdown# This IS Targeted### Next heading is an H3Use code with caution.❌ NOT TargetedBroken by a paragraph (Not immediate):html<h1>NOT Targeted</h1><p>This text breaks the adjacency.</p><h2>Following H2</h2>Use code with caution.Wrong heading level:html<h1>NOT Targeted</h1><h4>Next heading is an H4, which is not in our list</h4>Use code with caution.Not the first H1:html<h1>First H1 (followed by text)</h1><h1>NOT Targeted (followed by H2, but it is the second H1)</h1><h2>Sub-heading</h2>Use code with caution.Browser Support (2025)As of 2025, this selector is part of the "Baseline Widely Available" set. It is fully supported in all modern versions of Chrome, Firefox, Safari, and Edge. Can :has() select any sibling or only adjacent ones?Give an example of using :is() with :not()How does :first-of-type differ from :nth-child(1)?undefinedundefinedundefined8 siteshas() - CSS - MDN Web DocsDec 16, 2025 — Caution: This is a demo page. ... This example shows two similar texts side-by-side for comparison – the left one with...MDN Web DocsCSS :has() pseudo class - DEV CommunityJan 4, 2024 — Fortunately, the CSS :has() pseudo class is finally supported by all major browsers. Firefox took a while, but on Decem...DEV Community:has() CSS relational pseudo-class | Can I use ... - CanIUse121 - 145 Supported. 146 Supported. 147 - 149 Supported. Opera. 10 - 90 supported. 91 - 124 Supported. 125 Supported. IE. 6 - 10 s...CanIUseShow allhow can i copy this entire conversation in markdown?To copy this conversation in Markdown format, you can use one of the methods below. Since most AI interfaces do not have a single "Export to MD" button, Manual Selection is the most reliable way to preserve the formatting.Option 1: Manual Selection (Best for Formatting)Highlight all the text in this chat from the top to the bottom.Copy (Ctrl+C / Cmd+C).Paste (Ctrl+V / Cmd+V) into a Markdown editor like Obsidian, VS Code, or Typora.Note: Most modern Markdown editors will automatically recognize the headers, code blocks, and bold text during the paste process.Option 2: Use the "Copy" Button on Code BlocksFor the specific code solutions provided, you can click the Copy icon (usually a clipboard icon) located at the top-right corner of each black code box.Conversation Summary (Markdown Format)If you want to quickly paste the core logic into your notes, here is the summarized version of our conversation:markdown# CSS Selection Logic: H1 based on Following Headings## 1. Select H1 if ANY heading follows itTargets the first H1 if any H1-H6 exists later in the parent.```cssbody > h1:first-of-type:has(~ :is(h1, h2, h3, h4, h5, h6)) {  /* styles */}Use code with caution.2. Select H1 if an H2 is on the "next line"Targets the first H1 only if it is immediately followed by an H2.cssh1:first-of-type:has(+ h2) {  /* styles */}Use code with caution.3. Select H1 if an H2 OR H3 is on the "next line"Targets the first H1 only if the immediate next sibling is an H2 or H3.cssh1:first-of-type:has(+ :is(h2, h3)) {  /* styles */}Use code with caution.Browser Support (2025)Supported in: Chrome 105+, Safari 15.4+, Firefox 121+, Edge 105+.Status: Baseline Widely Available.### Tips for 2025*   **The `:has()` selector** is now the industry standard for "parent" or "previous sibling" selection.*   **The `:is()` selector** is the standard way to group multiple tags (like h1, h2, h3) inside a single selector to keep your code clean.Can I select the parent of an element using CSS today?Any alternatives to :has() for older browsers?Explain the difference between ~ and + combinators with examplesconvert this entire conversation to markdown for me to copyThinkingTo copy this conversation, highlight the text within the box below and copy it into any Markdown editor (like VS Code, Obsidian, or GitHub).markdown# Conversation: CSS Selectors for Complex Heading Relationships### User Query 1Only select this `<h1>` if it contains at least one other heading (`<h1>` through `<h6>`) after it.### Assistant ResponseIn standard CSS, it is **not possible** to select a preceding element based on a subsequent sibling without the `:has()` pseudo-class.**Modern CSS (2025):**```css/* Selects <h1> only if it is followed by another heading later in the parent */h1:has(~ :is(h1, h2, h3, h4, h5, h6)) {  color: red;}Use code with caution.User Query 2body > h1:first-of-type:has(~ h1, ~ h2, ~ h3, ~ h4, ~ h5, ~ h6) {}Assistant ResponseThis selector is correct and highly specific. It targets the first <h1> child of the <body>, provided it has a following sibling that is any heading level.User Query 3Give me multiple examples in HTML and multiple examples in Markdown that would and wouldn’t be targeted.Assistant Response✅ Targeted (HTML)html<body>  <h1>Targeted</h1>  <h2>Sub-heading</h2></body>Use code with caution.❌ NOT Targeted (HTML)html<body>  <h1>Not Targeted</h1>  <p>No other headings follow this.</p></body>Use code with caution.✅ Targeted (Markdown)markdown# This IS Targeted## Sub-headingUse code with caution.❌ NOT Targeted (Markdown)markdown# Not TargetedJust some text here.Use code with caution.User Query 4What would target only the first h1 that occurs where there is an h2 on the next line?Assistant ResponseUse the adjacent sibling combinator (+) inside the :has() pseudo-class:cssh1:first-of-type:has(+ h2) {  /* Styles */}Use code with caution.User Query 5What would target only the first h1 that occurs where there is an h2 or h3 on the next line?Assistant ResponseCombine the :has() selector with the :is() grouping pseudo-class for an "OR" condition:cssh1:first-of-type:has(+ :is(h2, h3)) {  /* Styles */}Use code with caution.Examples:Targeted: # Header followed immediately by ## Subheader.Not Targeted: # Header followed by a paragraph, then ## Subheader.Technical Summary (2025):has(): The relational pseudo-class used for "looking ahead."+: Targets the immediate next sibling ("next line").~: Targets any sibling that follows later.Support: Fully supported in all major evergreen browsers (Chrome, Firefox, Safari, Edge) as of 2025.{content: }____### CSS Selector Analysis: Specific H1 Targeting**Selector:** `body > h1:first-of-type:has(~ h1, ~ h2, ~ h3, ~ h4, ~ h5, ~ h6) {}`#### Breakdown of Functionality:1.  **`body > h1`**: Targets an `<h1>` that is a direct child of the `<body>`. It will not target an `<h1>` nested inside a `<main>`, `<section>`, or `<div>`.2.  **`:first-of-type`**: Ensures that only the **first** `<h1>` among its siblings is selected. Even if other `<h1>` elements meet the following criteria, they will be ignored.3.  **`:has(...)`**: The relational pseudo-class that allows the selector to check the siblings that follow.4.  **`~ h1, ~ h2, ...`**: The **subsequent-sibling combinator**. It checks if any heading (H1 through H6) exists anywhere after the first `<h1>` within the same parent container.#### Usage Example:```html<body>  <h1>This IS Targeted</h1>  <!-- First of type + followed by an H2 -->  <p>Introductory text.</p>  <h2>Secondary Heading</h2>    <h1>NOT Targeted</h1>       <!-- Followed by an H3, but NOT the first-of-type -->  <h3>Third Heading</h3></body>____### Targeting Examples: H1 Followed by Subsequent Headings**Selector:**`body > h1:first-of-type:has(~ h1, ~ h2, ~ h3, ~ h4, ~ h5, ~ h6)`---#### 1. HTML Examples##### ✅ Targeted*The H1 is a direct child of body, the first of its kind, and is followed by another heading.*```html<body>  <h1>This IS Targeted</h1>  <p>Some content.</p>  <h2>A sub-heading exists later</h2></body>____❌ NOT TargetedFails because no other heading exists after the H1.html<body>  <h1>NOT Targeted</h1>  <p>Only paragraphs follow this heading.</p></body>Use code with caution.Fails because the H1 is not a direct child of the body (it is wrapped in a <main> tag).html<body>  <main>    <h1>NOT Targeted</h1>    <h2>Following heading</h2>  </main></body>Use code with caution.2. Markdown ExamplesNote: In most Markdown renders, these headings become direct children of the container.✅ TargetedA standard document structure with multiple levels of headers. markdown# This IS TargetedThis is the first heading in the document.## Next SectionThe presence of this H2 triggers the selection of the H1 above.Use code with caution.❌ NOT TargetedFails because there is only one heading in the entire document.markdown# NOT TargetedThis is a standalone title with no other headings following it.Use code with caution.Fails because the H1 is not the first one. Only the first H1 in the document can be targeted.markdown# First Heading (No headings follow)# NOT Targeted## This H2 follows, but the H1 above is the second H1 in the file.Use code with caution.{content: }____To target only the first <h1> that is immediately followed by either an <h2> or an <h3>, use the CSS :has() pseudo-class combined with the adjacent sibling combinator (+) and the :is() grouping pseudo-class.	The Recommended CSS Selector (2025)```lang-cssh1:first-of-type:has(+ :is(h2, h3)) {  /* Your styles here */}```Use code with caution.Breakdown of how it works:h1:first-of-type: Ensures you only target the very first <h1> within its parent container.:has(...): Checks if the targeted element has a relationship with another element.+: The adjacent sibling combinator requires the next element to be on the immediate next "line" (next in the DOM).:is(h2, h3): Acts as an "OR" statement, matching if the next element is either an <h2> or an <h3>.Targeted vs. NOT Targeted Examples✅ TargetedHTML:```lang-html<body>  <h1>This IS Targeted</h1>  <h2>Next heading is an H2</h2></body>```Use code with caution.Markdown:```lang-md# This IS Targeted### Next heading is an H3```Use code with caution.❌ NOT TargetedBroken by a paragraph (Not immediate):```lang-html<h1>NOT Targeted</h1><p>This text breaks the adjacency.</p><h2>Following H2</h2>```Use code with caution.Wrong heading level:```lang-html<h1>NOT Targeted</h1><h4>Next heading is an H4, which is not in our list</h4>```Use code with caution.Not the first H1:```lang-html<h1>First H1 (followed by text)</h1><h1>NOT Targeted (followed by H2, but it is the second H1)</h1><h2>Sub-heading</h2>```Use code with caution.Browser Support (2025)As of 2025, this selector is part of the "Baseline Widely Available" set. It is fully supported in all modern versions of Chrome, Firefox, Safari, and Edge. Can :has() select any sibling or only adjacent ones?Give an example of using :is() with :not()How does :first-of-type differ from :nth-child(1)?has() - CSS - MDN Web DocsDec 16, 2025 — Caution: This is a demo page. ... This example shows two similar texts side-by-side for comparison – the left one with...MDN Web DocsCSS :has() pseudo class - DEV CommunityJan 4, 2024 — Fortunately, the CSS :has() pseudo class is finally supported by all major browsers. Firefox took a while, but on Decem...DEV Community:has() CSS relational pseudo-class | Can I use ... - CanIUse121 - 145 Supported. 146 Supported. 147 - 149 Supported. Opera. 10 - 90 supported. 91 - 124 Supported. 125 Supported. IE. 6 - 10 s...CanIUseFeatures - State of CSS 2025All Features. Of all the features included in the survey, :has() is both the most-used and the most-loved one. Also worth pointing...State of CSS 2025has() CSS relational pseudo-class - LambdaTestNov 11, 2025 — :has() CSS relational pseudo-class on Chrome is fully supported on 106-145, and not supported on 4-105 Chrome versions...LambdaTestScreencast: CSS's Adjacent Sibling Selector - SitePointMar 1, 2024 — What is the CSS Adjacent Sibling Selector and how does it work? The CSS Adjacent Sibling Selector is a combinator in CS...SitePointThe CSS :has Selector (and 4+ Examples)Mar 17, 2021 — … and also :is() and :where() CSS Selectors Level 4 is also the same spec that has the :is selector that can be used l...CSS-Tricks:first-of-type | CSS-TricksSep 13, 2024 — :first-of-type. ... The :first-of-type selector in CSS allows you to target the first occurence of an element within i...CSS-Trickshow____