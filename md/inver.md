---title: Color Inversion | CSS & JavaScriptauthor: ELHcategories:	- CSS	- JStags:	- color inversion  - css  - jskeywords:		js, css, color inversioncss:			../assets/demomd.mmd.cssxhtml header:	<link rel="stylesheet" type="text/css" media="all" href="../assets/prism84blue.css"/>	<script type="text/javascript" src="../assets/prism.js"></script>	<link href="../assets/demomd.mmd.css" rel="stylesheet" type="text/css" media="all" />	<style>pre code, code,pre{white-space:pre-line !important; } </style>favicon: ../assets/icons/favicon.svgimage: ../assets/icons/favicon.svglink-citations: TRUEtableOfContents: TRUElastUpdated: TRUE---# color inversion JavaScript## invert color on all elements of a page with JavaScript.Sometimes, we want to invert color on all elements of a page with JavaScript. In this article, we’ll look at how to invert color on all elements of a page with JavaScript. Invert Color on All Elements of a Page with JavaScriptTo invert color on all elements of a page with JavaScript, we just set use the `invert(100%)` value with CSS.For instance, if we have:```lang-html<div style='background-color: white'>  hello</div>```Then we can invert all the colors by writing:```lang-javaconst css = `html {  -webkit-filter: invert(100%);  -moz-filter: invert(100%);  -o-filter: invert(100%);  -ms-filter: invert(100%); }`const head = document.headconst style = document.createElement('style')style.type = 'text/css';if (style.styleSheet) {  style.styleSheet.cssTextconst css = `html {  -webkit-filter: invert(100%);  -moz-filter: invert(100%);  -o-filter: invert(100%);  -ms-filter: invert(100%); }`const head = document.headconst style = document.createElement('style')style.type = 'text/css';if (style.styleSheet) {  style.styleSheet.cssText = css;} else {  style.appendChild(document.createTextNode(css));}head.appendChild(style); = css;} else {  style.appendChild(document.createTextNode(css));}head.appendChild(style);```1. We create the css string to set the filter property to `invert(100%)` on the html element.2. Then we get the head element with `document.head` .3. Then we create the style element with `document.createElement` .4. We set the type attribute of style by assigning a value to `style.type` .5. And then we apply the styles from the css string with:```lang-javaif (style.styleSheet) {  style.styleSheet.cssText = css;} else {  style.appendChild(document.createTextNode(css));}head.appendChild(style);```to set the content of the style element and append it to the head with `head.appendChild` .## ConclusionTo invert color on all elements of a page with JavaScript, we just set use the `invert(100%)` value with CSS.____```lang-jsdef invert_color_int(color_int): # Assuming 0xAARRGGBB format  alpha = (color_int >> 24) & 0xFF  red = (color_int >> 16) & 0xFF  green = (color_int >> 8) & 0xFF  blue = color_int & 0xFF inverted_red = 255 - red inverted_green = 255 - green inverted_blue = 255 - blue inverted_color_int = (alpha << 24) | (inverted_red << 16) | \(inverted_green << 8) | inverted_blue  return inverted_color_int# Example usageoriginal_color_int = 0xFFC83296 # Example color with alphainverted_color_int = invert_color_int(original_color_int)print(f"Original: {hex(original_color_int)}, Inverted: {hex(inverted_color_int)}")``````lang-jsdef invert_rgb_color(r, g, b):  new_r = 255 - r  new_g = 255 - g  new_b = 255 - b  return (new_r, new_g, new_b)```# Example usage```lang-jsoriginal_color = (200, 50, 150)inverted_color = invert_rgb_color(*original_color)print(f"Original: {original_color}, Inverted: {inverted_color}")``````lang-cssbody {  filter: invert(100%); /* Inverts all colors on the entire page */}``````lang-css/* Or for specific elements */.my-element {  filter: invert(100%);}``````lang-js// Inverted color: rgb(255 - x, 255 - y, 255 - z)    document.documentElement.style.filter = 'invert(1)';    html {      filter: invert(1);    }    /* Inverts all colors of the element it's applied to */    filter: invert(1);// Example fragment shader for color inversionvoid fragment() {    vec4 screen_color = texture(SCREEN_TEXTURE, SCREEN_UV);    COLOR = vec4(1.0 - screen_color.rgb, screen_color.a);}def invert_color_int(color_int):  # Assuming 0xAARRGGBB format  alpha = (color_int >> 24) & 0xFF  red = (color_int >> 16) & 0xFF  green = (color_int >> 8) & 0xFF  blue = color_int & 0xFF  inverted_red = 255 - red  inverted_green = 255 - green  inverted_blue = 255 - blue  inverted_color_int = (alpha << 24) | (inverted_red << 16) | \                       (inverted_green << 8) | inverted_blue  return inverted_color_int```# Example usage```lang-jsoriginal_color_int = 0xFFC83296 # Example color with alphainverted_color_int = invert_color_int(original_color_int)print(f"Original: {hex(original_color_int)}, Inverted: {hex(inverted_color_int)}")def invert_rgb_color(r, g, b):  new_r = 255 - r  new_g = 255 - g  new_b = 255 - b  return (new_r, new_g, new_b)# Example usageoriginal_color = (200, 50, 150)inverted_color = invert_rgb_color(*original_color)print(f"Original: {original_color}, Inverted: {inverted_color}")``````language-cssbody {  filter: invert(100%); /*Inverts all colors on the entire page*/}``````language-css/* Or for specific elements */.my-element {  filter: invert(100%);}``````language-bshconvert input.png -negate output.png``````language-js    // Assuming RGBA format (alpha is in the leftmost byte)    uint invertedColor = ~rgbaColor;``````language-Python    // Original color: rgb(x, y, z)    // Inverted color: rgb(255 - x, 255 - y, 255 - z)``````lang-js    document.documentElement.style.filter = 'invert(1)';``````lang-csshtml {    filter: invert(1);}``````lang-css/* Inverts all colors of the element it's applied to */filter: invert(1);``` ```lang-js// Example fragment shader for color inversionvoid fragment() {    vec4 screen_color = texture(SCREEN_TEXTURE, SCREEN_UV);    COLOR = vec4(1.0 - screen_color.rgb, screen_color.a);}``````language-Python// Original color: rgb(x, y, z)// Inverted color: rgb(255 - x, 255 - y, 255 - z)``````language-jsdocument.documentElement.style.filter = 'invert(1)';``````language-csshtml {  filter: invert(1);}``````language-css/* Inverts all colors of the element it's applied to */filter: invert(1);``````language-js// Example fragment shader for color inversionvoid fragment() {    vec4 screen_color = texture(SCREEN_TEXTURE, SCREEN_UV);    COLOR = vec4(1.0 - screen_color.rgb, screen_color.a);}``````language-css.theme-dark span[src$="invert_B"] img {	filter: invert(1) hue-rotate(180deg);}``````language-css.theme-dark img[alt$="invert_dark"] {	filter: invert(1);}``````language-css.theme-dark div[src$="#invert"] img,.theme-dark span[src$="#invert"] img {	filter: invert(1) hue-rotate(180deg);	mix-blend-mode: screen;}```