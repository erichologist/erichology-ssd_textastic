
input = input.replace(/🄿︎/gm, '%F0%9F%84%BF%EF%B8%8E');
input = input.replace(/⃞/gm, '%E2%83%9E');
input = input.replace(/⃞/gm, '%E2%83%9E');
input = input.replace(/⇢[ͳ]⇠/gm, '%E2%87%A2%5B%CD%B3%5D%E2%87%A0');
input = input.replace(/[𝚨⃞]/gm, '%5B%F0%9D%9A%A8%E2%83%9E%5D');
input = input.replace(/◐/gm, '%E2%97%90');
input = input.replace(/ͤ/gm, '%CD%A4');
input = input.replace(/ɪᴍɢ/gm, '%C9%AA%E1%B4%8D%C9%A2');
input = input.replace(/🄿︎/gm, '%F0%9F%84%BF%EF%B8%8E');
input = input.replace(/⃞/gm, '%E2%83%9E');
input = input.replace(/⃞/gm, '%E2%83%9E');
input = input.replace(/🄿︎/gm, '%F0%9F%84%BF%EF%B8%8E');
input = input.replace(/⃞/gm, '%E2%83%9E');
input = input.replace(/ᴜʀʟ/gm, '%E1%B4%9C%CA%80%CA%9F');
input = input.replace(/﹝/gm, '%EF%B9%9D');
input = input.replace(/﹞/gm, '%EF%B9%9E');
input = input.replace(/→/gm, '%E2%86%92');
input = input.replace(/↺/gm, '%E2%86%BA');
input = input.replace(/↻/gm, '%E2%86%BB');
input = input.replace(/⇠/gm, '%E2%87%A0');
input = input.replace(/⇢/gm, '%E2%87%A2');
input = input.replace(/⇦/gm, '%E2%87%A6');
input = input.replace(/⇧/gm, '%E2%87%A7');
input = input.replace(/⇨/gm, '%E2%87%A8');
input = input.replace(/⇩/gm, '%E2%87%A9');
input = input.replace(/⊕/gm, '%E2%8A%95');
input = input.replace(/⌘/gm, '%E2%8C%98');
input = input.replace(/⌨︎/gm, '%E2%8C%A8%EF%B8%8E');
input = input.replace(/⌨️/gm, '%E2%8C%A8%EF%B8%8F');
input = input.replace(/⎌/gm, '%E2%8E%8C');
input = input.replace(/▵/gm, '%E2%96%B5');
input = input.replace(/◐/gm, '%E2%97%90');
input = input.replace(/✂️/gm, '%E2%9C%82%EF%B8%8F');
input = input.replace(/⭐️/gm, '%E2%AD%90%EF%B8%8F');
input = input.replace(/🌎/gm, '%F0%9F%8C%8E');
input = input.replace(/🌐/gm, '%F0%9F%8C%90');
input = input.replace(/🎙/gm, '%F0%9F%8E%99');
input = input.replace(/🎬/gm, '%F0%9F%8E%AC');
input = input.replace(/🏞/gm, '%F0%9F%8F%9E');
input = input.replace(/🐙/gm, '%F0%9F%90%99');
input = input.replace(/👵🏼/gm, '%F0%9F%91%B5%F0%9F%8F%BC');
input = input.replace(/💻/gm, '%F0%9F%92%BB');
input = input.replace(/💾/gm, '%F0%9F%92%BE');
input = input.replace(/📋/gm, '%F0%9F%93%8B');
input = input.replace(/📌/gm, '%F0%9F%93%8C');
input = input.replace(/📝/gm, '%F0%9F%93%9D');
input = input.replace(/📡/gm, '%F0%9F%93%A1');
input = input.replace(/📰/gm, '%F0%9F%93%B0');
input = input.replace(/📱/gm, '%F0%9F%93%B1');
input = input.replace(/📸/gm, '%F0%9F%93%B8');
input = input.replace(/🔉/gm, '%F0%9F%94%89');
input = input.replace(/🔌/gm, '%F0%9F%94%8C');
input = input.replace(/🔗/gm, '%F0%9F%94%97');
input = input.replace(/🕦/gm, '%F0%9F%95%A6');
input = input.replace(/🗳️/gm, '%F0%9F%97%B3%EF%B8%8F');
input = input.replace(/🤖/gm, '%F0%9F%A4%96');
input = input.replace(/🦹‍♂️/gm, '%F0%9F%A6%B9%E2%80%8D%E2%99%82%EF%B8%8F');
input = input.replace(/🧭/gm, '%F0%9F%A7%AD');
input = input.replace(/🧮/gm, '%F0%9F%A7%AE');
input = input.replace(/🧸/gm, '%F0%9F%A7%B8');
input = input.replace(/🪄/gm, '%F0%9F%AA%84');
input = input.replace(/⿻/gm, '%E2%BF%BB');
input = input.replace(/²/gm, '%C2%B2');
input = input.replace(/ᴀ/gm, '%E1%B4%80');
input = input.replace(/ᵉ/gm, '%E1%B5%89');
input = input.replace(/ₑ/gm, '%E2%82%91');
input = input.replace(/𝐄︎/gm, '%F0%9D%90%84%EF%B8%8E');
input = input.replace(/ᴱ/gm, '%E1%B4%B1');
input = input.replace(/🄴/gm, '%F0%9F%84%B4');
input = input.replace(/ᴳ/gm, '%E1%B4%B3');
input = input.replace(/ɢ/gm, '%C9%A2');
input = input.replace(/i⃣/gm, 'i%E2%83%A3');
input = input.replace(/ɪ/gm, '%C9%AA');
input = input.replace(/ʟ/gm, '%CA%9F');
input = input.replace(/ᴍ/gm, '%E1%B4%8D');
input = input.replace(/🄿︎/gm, '%F0%9F%84%BF%EF%B8%8E');
input = input.replace(/ʀ/gm, '%CA%80');
input = input.replace(/ˢ/gm, '%CB%A2');
input = input.replace(/ₛ/gm, '%E2%82%9B');
input = input.replace(/🆂︎/gm, '%F0%9F%86%82%EF%B8%8E');
input = input.replace(/𝐓︎/gm, '%F0%9D%90%93%EF%B8%8E');
input = input.replace(/ᴜ/gm, '%E1%B4%9C');
input = input.replace(/𝛂⃞/gm, '%F0%9D%9B%82%E2%83%9E');
input = input.replace(/ͳ/gm, '%CD%B3');
input = input.replace(/ꭼ/gm, '%EA%AD%BC');
input = input.replace(/ꮪ/gm, '%EA%AE%AA');
input = input.replace(/ᔆ/gm, '%E1%94%86');
input = input.replace(//gm, '%EF%A1%81');
input = input.replace(//gm, '%EF%A3%A9');
input = input.replace(//gm, '%EF%A3%BF');
input = input.replace(/./gm, '.');


